APP_ENV=beta

# Supabase chevere_plan_pdn → Settings → API → Project URL
# (GitHub Pages: secret SUPABASE_URL_PDN con el mismo valor)
SUPABASE_URL_PDN=
# Supabase chevere_plan_pdn → Settings → API → anon public
# (GitHub Pages: secret SUPABASE_ANON_KEY_PDN con el mismo valor)
SUPABASE_ANON_KEY_PDN=
